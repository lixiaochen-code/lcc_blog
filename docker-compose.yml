services:
  mysql:
    image: mysql:8.4
    container_name: lcc-blog-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: lcc_blog
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: lcc
      MYSQL_PASSWORD: lcc
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7.4
    container_name: lcc-blog-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  mysql_data:
  redis_data:
